apiVersion: v1
kind: ResourceQuota
metadata:
  name: compute-resources
spec:
  hard:
    pods: "5" 
    requests.cpu: "500m" 
    requests.memory: "500Mi" 
    limits.cpu: "500m" 
    limits.memory: "500Mi" 
